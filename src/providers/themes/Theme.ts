export type Theme = {
  colors: {
    text: {
      primary: string
      secondary: string
    },
    background: {
      primary: string
      secondary: string
    },
    card: {
      background: string
    },
    shadow: string
  }
}